## Reconciliation export has no defined column set
Observed  ui/Reconcile.jsx:16 calls an export endpoint that does not exist
Costs     nothing today; the pilot reads the variance on screen
Closes    one sentence naming who reads the export and what they do with it
Raised    2026-09-02 - user deferred, see decisions.md
