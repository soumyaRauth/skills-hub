# Billing

We use Stripe Checkout — customers are redirected to a Stripe-hosted page and we
never see a card number. Stripe sends us webhooks when a subscription changes.

Refunds are issued from the admin panel by support staff.
