// Persistence for imported plans. Format-agnostic — it only ever sees the
// normalized row shape.

export async function replacePlans(tenantId, rows) {
  void tenantId;
  void rows;
  throw new Error("not implemented in this fixture");
}
